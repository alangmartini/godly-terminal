{
  "name": "godly-terminal",
  "version": "0.17.0",
  "private": true,
  "license": "BUSL-1.1",
  "scripts": {
    "unlock": "node scripts/unlock-binaries.js",
    "dev": "cd src-tauri && cargo build -p godly-daemon && cargo run -p godly-iced-shell",
    "build:daemon": "node scripts/unlock-binaries.js && cd src-tauri && cargo build -p godly-daemon",
    "build:daemon:release": "node scripts/unlock-binaries.js --release && cd src-tauri && cargo build -p godly-daemon --release",
    "build:mcp": "node scripts/unlock-binaries.js && cd src-tauri && cargo build -p godly-mcp",
    "build:mcp:release": "node scripts/unlock-binaries.js --release && cd src-tauri && cargo build -p godly-mcp --release",
    "build:notify": "node scripts/unlock-binaries.js && cd src-tauri && cargo build -p godly-notify",
    "build:notify:release": "node scripts/unlock-binaries.js --release && cd src-tauri && cargo build -p godly-notify --release",
    "build:remote": "node scripts/unlock-binaries.js && cd src-tauri && cargo build -p godly-remote",
    "build:remote:release": "node scripts/unlock-binaries.js --release && cd src-tauri && cargo build -p godly-remote --release",
    "build:whisper": "node scripts/unlock-binaries.js && cd src-tauri && cargo build -p godly-whisper",
    "build:whisper:release": "node scripts/unlock-binaries.js --release && cd src-tauri && cargo build -p godly-whisper --release",
    "build:native": "pwsh scripts/build-native.ps1",
    "build:native:release": "pwsh scripts/build-native.ps1 -Release",
    "build:production": "pwsh scripts/production-build.ps1",
    "test:rust": "cd src-tauri && cargo nextest run --workspace",
    "test:rust:fast": "cd src-tauri && cargo nextest run --workspace --profile fast",
    "test:smart": "bash scripts/test-smart.sh",
    "test:yaml": "node godly-test/godly-test.mjs run tests/e2e-yaml/",
    "test:yaml:validate": "node godly-test/godly-test.mjs validate tests/e2e-yaml/",
    "test:yaml:list": "node godly-test/godly-test.mjs list tests/e2e-yaml/",
    "version:bump": "node scripts/bump-version.mjs",
    "phone": "pwsh scripts/setup-phone.ps1"
  },
  "devDependencies": {
    "qrcode-terminal": "^0.12.0"
  }
}
