{
  "name": "godly-terminal",
  "version": "0.13.0",
  "private": true,
  "license": "BUSL-1.1",
  "scripts": {
    "unlock": "node scripts/unlock-binaries.js",
    "dev": "cd src-tauri && cargo build -p godly-daemon && cargo run -p godly-iced-shell",
    "build:daemon": "pnpm run unlock && cd src-tauri && cargo build -p godly-daemon",
    "build:daemon:release": "pnpm run unlock -- --release && cd src-tauri && cargo build -p godly-daemon --release",
    "build:mcp": "pnpm run unlock && cd src-tauri && cargo build -p godly-mcp",
    "build:mcp:release": "pnpm run unlock -- --release && cd src-tauri && cargo build -p godly-mcp --release",
    "build:notify": "pnpm run unlock && cd src-tauri && cargo build -p godly-notify",
    "build:notify:release": "pnpm run unlock -- --release && cd src-tauri && cargo build -p godly-notify --release",
    "build:remote": "pnpm run unlock && cd src-tauri && cargo build -p godly-remote",
    "build:remote:release": "pnpm run unlock -- --release && cd src-tauri && cargo build -p godly-remote --release",
    "build:whisper": "pnpm run unlock && cd src-tauri && cargo build -p godly-whisper",
    "build:whisper:release": "pnpm run unlock -- --release && cd src-tauri && cargo build -p godly-whisper --release",
    "build:native": "pwsh scripts/build-native.ps1",
    "build:native:release": "pwsh scripts/build-native.ps1 -Release",
    "test:rust": "cd src-tauri && cargo nextest run --workspace",
    "test:rust:fast": "cd src-tauri && cargo nextest run --workspace --profile fast",
    "test:smart": "bash scripts/test-smart.sh",
    "version:bump": "node scripts/bump-version.mjs",
    "phone": "pwsh scripts/setup-phone.ps1",
    "production:build": "pwsh scripts/production-build.ps1",
    "production:install": "pwsh scripts/production-install.ps1"
  },
  "devDependencies": {
    "qrcode-terminal": "^0.12.0"
  }
}
