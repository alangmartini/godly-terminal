[package]
name = "godly-protocol"
version = "0.1.0"
edition = "2021"
license = "BUSL-1.1"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
criterion = "0.5"

[[bench]]
name = "snapshot_serialization"
harness = false
