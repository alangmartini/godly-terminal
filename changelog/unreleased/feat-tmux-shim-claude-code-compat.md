### Added
- **tmux environment injection** — Terminal sessions now include `TMUX` and `TMUX_PANE` environment variables, enabling Claude Code's Agent Teams to detect Godly Terminal as a tmux-compatible environment.
