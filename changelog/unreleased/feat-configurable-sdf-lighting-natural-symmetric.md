### Changed
- **DirectWrite rendering mode reverted to NATURAL_SYMMETRIC** — re-enables symmetric anti-aliasing for text glyphs now that the gamma compositing pipeline is corrected, matching Chrome/Skia's default on Windows
