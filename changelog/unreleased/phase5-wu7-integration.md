### Added

- **Workspace system integration** — Wire workspaces, sidebar, settings dialog, shortcuts tab, and notifications into the native shell app. Each workspace owns its own layout tree and focused terminal. Ctrl+Alt+Right/Left to switch, Ctrl+B to toggle sidebar, Ctrl+, for settings.
