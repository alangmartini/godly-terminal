### Fixed

- **Remove sidebar session list bottom fade gradient** — Removed the 16px gradient overlay at the bottom of the sidebar session list that darkened the last session item. The web reference uses no such overlay, so this brings native rendering closer to visual parity.
