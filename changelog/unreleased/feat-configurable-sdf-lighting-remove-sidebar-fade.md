### Fixed

- **Remove sidebar session list bottom fade gradient** — removed the 16px gradient overlay at the bottom of the session list that darkened the last session item. The web reference uses no such overlay, achieving closer visual parity.
