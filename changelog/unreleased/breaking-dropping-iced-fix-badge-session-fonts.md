### Fixed

- **Fix tab badge and session number font rendering** — Tab badge count text ("3", "12") was incorrectly using monospace font at 14px; now renders at 9px proportional bold matching web reference's Badge component. Session ID numbers (1, 2, 3) were similarly using monospace at 14px; now render at 12px proportional matching web reference.
