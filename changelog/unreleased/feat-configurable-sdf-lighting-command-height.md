### Fixed

- **Command block layout height** — Added 2px border height to `COMMAND_HEIGHT` constant in reference layout, matching web CSS `border: "1px solid #1e2128"` which contributes 1px top + 1px bottom to total element height.
