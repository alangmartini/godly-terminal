### Added
- **ContentPane layout node** — layout-core now supports non-terminal pane types (code viewer, markdown preview, image viewer) via `PaneContent`, `FileViewerType`, and the `ContentPane` variant on `LayoutNode`
