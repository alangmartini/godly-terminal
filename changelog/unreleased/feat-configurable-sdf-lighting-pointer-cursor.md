### Added

- **Pointer cursor on interactive elements** — Mouse cursor changes to pointer (hand) when hovering over tabs, session items, close buttons, and other clickable UI elements, matching the web reference `cursor: "pointer"` CSS behavior.
