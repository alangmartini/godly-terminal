### Added
- **tmux shim pane commands** — `split-window`, `select-pane`, `list-panes`, `kill-pane` commands for the `godly-tmux-shim` binary that translates tmux CLI into Godly Terminal MCP operations
