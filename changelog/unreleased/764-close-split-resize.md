### Fixed
- **Terminal remains half-size after closing split partner** — when closing one terminal from a split layout, the remaining terminal now correctly resizes to fill the full viewport instead of staying at its previous half-size (#764)
