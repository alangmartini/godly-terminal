### Added

- **Streaming cursor blink animation** — Reference pane cursor now blinks with a 1s cycle (0.5s on, 0.5s off) matching the web reference `animation: blink 1s infinite` keyframes.

### Changed

- **Tilde pulse animation tuning** — Status bar streaming `~` indicator pulse adjusted to use cosine wave for smoother 1.5s cycle matching web reference keyframes.
