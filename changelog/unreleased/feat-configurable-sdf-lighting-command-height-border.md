### Fixed

- **Command block layout height** — Added 2px border height to COMMAND_HEIGHT constant (30px → 32px) to match web reference's `border: 1px solid #1e2128` which contributes 2px to total layout height.
