### Fixed

- **SDF quad dither noise removed** — Removed per-pixel dither_noise from the SDF fragment shader output path so rounded rects, badges, borders, and session cards render with exact flat CSS colors matching the web reference.
