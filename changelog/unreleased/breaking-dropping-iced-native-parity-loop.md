### Added
- **Native parity workflow** — Added OMX visual-parity loop scripts, reference screenshots, and supporting docs so native `godly-shell` parity work can continue across machines on the `breaking/dropping-iced` branch

### Changed
- **Godly shell parity surface** — Expanded the native shell’s retained layout, reference-scene rendering, text/layout helpers, and parity harness artifacts for continued web-reference matching
