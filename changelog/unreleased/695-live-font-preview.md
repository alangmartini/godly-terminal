### Added
- **In-dialog font preview** — Appearance tab now shows a live preview section with sample terminal text that updates instantly when font family or size changes (#695)
- **Font change diagnostics** — Added `log::info!` calls to font change handlers for debugging font propagation

### Changed
- **Font selection UI** — Current font selection now indicated with a checkmark in the Appearance tab
