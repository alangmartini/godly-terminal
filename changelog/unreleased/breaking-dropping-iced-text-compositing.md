### Changed
- **Alpha-composited UI text rendering** — UI chrome text now uses grayscale AA with alpha blending instead of opaque ClearType, eliminating rectangular background artifacts on gradient and SDF-rounded surfaces
- **SDF cursor rendering** — terminal cursor now renders through the quad pipeline with rounded corners and a subtle blue glow for professional polish
