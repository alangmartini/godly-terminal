### Added
- **Enhanced performance overlay** — per-phase render breakdown (BG, glyph, cursor, selection), dropped frame detection with red highlight, frame time histogram buckets, and cell count display in the Ctrl+Shift+O overlay
