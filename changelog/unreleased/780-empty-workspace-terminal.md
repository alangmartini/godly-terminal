### Fixed

- **Empty workspaces get unwanted terminal on restart** — when a workspace is intentionally empty (user closed all terminals before shutdown), the app no longer creates a new terminal on restart (#771)
