### Added
- **Welcome screen hero icon** — Large SDF-rendered terminal monitor icon with accent-tinted halo glow above the "Godly Terminal" title for prominent branding presence
- **Content area edge vignettes** — Soft gradient shadows along top, left, and bottom edges for cinematic depth framing
- **Welcome card container drop shadow** — Floating depth effect below the shortcut card container
- **Version indicator** — Muted build version string (`v{CARGO_PKG_VERSION}`) below welcome shortcut cards

### Changed
- **SDF chevron icon rendering** — Terminal icon prompt chevron uses SDF rotated pills instead of rasterized line segments for smooth anti-aliased scaling at any size
