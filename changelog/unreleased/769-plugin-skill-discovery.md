### Fixed

- **Plugin skills nested under `.claude/`** — Quick Claude now discovers plugin skills stored at `<installPath>/.claude/skills/` and commands at `<installPath>/.claude/commands/`. Plugins like `ui-ux-pro-max` that follow Claude Code conventions will now appear in Quick Claude autocomplete (#769)
