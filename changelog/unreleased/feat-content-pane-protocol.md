### Added
- **ContentPane layout variant** — protocol supports non-terminal content panes (code viewer, markdown preview, image viewer) in the split-pane layout tree
- **Content pane MCP tools** — `OpenFilePane`, `ClosePane`, `ListPanes`, and `UpdateFilePane` request types with `PaneCreated` and `PaneList` responses
