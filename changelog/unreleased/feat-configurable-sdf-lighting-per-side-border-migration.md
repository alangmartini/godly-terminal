### Changed

- **Active indicators use per-side SDF borders** — Active session left border and active tab bottom accent now render as single `fill_rounded_border_sides` calls instead of separate overlay rectangles. The left border follows the session card's rounded corners naturally.
