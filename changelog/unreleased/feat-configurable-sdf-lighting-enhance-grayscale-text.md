### Fixed

- **Bolder grayscale AA text** — Apply enhanced contrast boost to grayscale text alpha path in atlas shader, matching the weight boost already applied to ClearType subpixel channels. All UI chrome text (sidebar, tabs, status bar) now renders bolder, closer to browser text weight.
