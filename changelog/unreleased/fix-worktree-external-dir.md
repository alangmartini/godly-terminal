### Fixed
- **Quick Claude worktree navigation** — Moved worktrees from `<repo_root>/.godly-worktrees/` to `%APPDATA%/com.godly.terminal/worktrees/` to avoid git-inside-git nesting that caused Claude agents to navigate back to parent repo
