### Added
- **Superellipse (squircle) corner smoothness in SDF shader** — configurable `corner_smoothness` parameter (0.0 = circular CSS arcs, 1.0 = full squircle with Apple/iOS continuous curvature) threaded through all SDF quad pipelines and UiBuilder API
