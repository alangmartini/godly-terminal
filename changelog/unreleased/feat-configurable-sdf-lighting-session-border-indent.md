### Fixed
- **Session item content indentation** — Sidebar session text (number, name, branch) now accounts for the 3px CSS `borderLeft` space, shifting content 3px right to match web reference's `borderLeft: "3px solid transparent"` + `padding: "7px 8px"` = 11px total left indent.
