### Added
- **tmux shim I/O commands** — `send-keys` and `display-message` commands for the `godly-tmux-shim` binary, translating tmux CLI calls into Godly Terminal daemon pipe operations for Claude Code Agent Teams
