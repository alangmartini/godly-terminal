### Added
- **End-to-end pipeline benchmark** - Criterion benchmark measuring the full VT parse -> grid conversion -> pixel render pipeline at 80x24 and 120x40 sizes, plus incremental and conversion-only benchmarks
