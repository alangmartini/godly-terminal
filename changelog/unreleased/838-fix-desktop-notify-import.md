### Fixed
- **Desktop notification preferences import path** — use `godly_iced_shell::desktop_notify_prefs` instead of `crate::desktop_notify_prefs` in `app.rs`, fixing compilation for the binary crate (#838)
