### Fixed
- **Quick Claude trust prompt not auto-accepted** — Quick Claude now detects and dismisses the Claude Code workspace trust prompt during launch, preventing the session from getting stuck waiting for user input
