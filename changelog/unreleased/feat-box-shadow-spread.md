### Added

- **CSS box-shadow spread support** — New `fill_shadow_css()` and `fill_shadow_css_custom()` methods implement the CSS `box-shadow: dx dy blur spread color` model. Spread expands or contracts the shadow shape independently of blur, enabling more accurate CSS box-shadow replication.
