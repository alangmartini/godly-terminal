### Fixed
- **Quick Claude image command parsing** — fixed PowerShell misinterpreting multi-line command strings with embedded newlines on Windows PTY; image references now kept on a single line in the prompt text
