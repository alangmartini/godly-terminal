### Changed
- **Solid border separators** — All panel borders (sidebar, tab bar, status bar) now use solid 1px #1a1d25 matching the web reference, replacing ultra-low opacity borders (0.12-0.50)
- **Flat sidebar surface** — Removed gradient, inner shadow, and inward shadow from sidebar for clean flat appearance matching web reference
- **Flat tab bar** — Removed 2px accent stripe at top and branding gradient for clean flat look
- **Git diff colorization** — Status bar now shows colorized +N -M format instead of raw --shortstat output
