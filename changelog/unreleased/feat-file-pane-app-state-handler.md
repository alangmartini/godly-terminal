### Added
- **File pane state management** - app-side `FilePaneState` struct, `ContentPane` layout node variant, and file watching via 500ms polling subscription
- **MCP file pane tools** - `open_file_pane`, `close_pane`, `list_panes`, and `update_file_pane` request handlers with `PaneCreated`/`PaneList` responses
