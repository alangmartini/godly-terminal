### Changed
- **Tab bar color harmony** — Active tab background now matches terminal content (PANE_BG) instead of contrasting; inactive tabs and bar use BG_SECONDARY for visual hierarchy
- **Tab border refinement** — Removed 2px accent border from active tab for cleaner design
