### Changed
- **Switch UI sans font to grayscale AA** — UI chrome text (sidebar, tab bar, status bar) now uses grayscale anti-aliasing and sRGB gamma 2.2 compositing instead of ClearType subpixel AA with gamma 1.8, matching browser text rendering weight
