### Changed

- **sRGB-correct gradient interpolation** — GPU now interpolates vertex colors in linear space, producing perceptually correct gradients across all UI elements (tab underlines, progress bars, hover transitions, sidebar glows, etc.)
