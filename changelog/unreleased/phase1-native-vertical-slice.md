### Added
- **Native Terminal Vertical Slice (Phase 1)** — Interactive single-terminal window via Iced 0.14 + wgpu, connecting to the same daemon as the Tauri app. Includes bridge I/O thread, Canvas-based grid rendering, keyboard input mapping, and integration test harness.

### Changed
- **Iced version bumped from 0.13 to 0.14** — Enables wgpu 27, Canvas widget, and reactive rendering for the native frontend crates.
