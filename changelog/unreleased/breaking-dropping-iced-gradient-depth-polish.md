### Changed
- **Gradient depth polish** — All interactive UI elements (pills, hover states, panels) now use subtle top-to-bottom gradient fills for 3D depth instead of flat colors
- **Enhanced active tab glow** — Active tab has full-tab ambient luminosity from its accent color, creating a stronger visual selection cue
- **Sidebar active item ambient glow** — Active sidebar item radiates subtle accent-colored aura behind its container
- **Window double-border** — Window frame uses dark outer + subtle bright inner border for a more solid professional edge
- **Scrollbar depth** — Scrollbar thumb gains shadow and inner highlight on hover proximity for glossy feel
- **Agent panel separators** — Subtle faded separator lines between process items in sidebar agent panel
