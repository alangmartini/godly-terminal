### Added
- **Cloudflare Tunnel setup UI** — configure and manage Cloudflare Tunnel directly from Settings > Remote, with Quick Tunnel (temporary URL, no account needed) and Named Tunnel (persistent hostname) modes
- **Cloudflare Access email protection** — restrict tunnel access to a single email via one-time code, configured through the Cloudflare API from within the settings UI
