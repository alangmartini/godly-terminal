### Added

- **Cursor style rendering** — Full pipeline from VT parser (DECSCUSR sequence CSI Ps SP q) to Iced surface renderer supporting block, underline, and bar cursor shapes with blink/steady variants
