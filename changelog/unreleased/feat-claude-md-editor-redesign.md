### Changed
- **CLAUDE.md editor modal redesign** — modernized the appearance with cleaner layout, improved visual hierarchy, distinct panes (darker editor, lighter preview), accent-colored UI elements, and better visual distinction between markdown elements (H1 underline, H2 tint, code block left border, styled bullet points, numbered lists)
- **Editor interface improvements** — added SOURCE/PREVIEW pane labels, cleaner header with contextual save button (shows "Saved" when clean, accent-highlighted "Save" when dirty), borderless close button, footer with path and modified indicator
- **Markdown preview enhancements** — better spacing between items, subtle custom separators for horizontal rules, added mix() color utility for theme color blending, helpful empty state message, softer modal shadow and stronger backdrop for better focus
- **Dialog styling** — increased border radius to 16px for modern appearance, improved pane styling with 10px border radius and distinct backgrounds
