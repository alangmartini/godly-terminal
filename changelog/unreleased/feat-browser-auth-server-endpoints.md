### Added
- **Browser password authentication** — added `POST /api/authenticate` endpoint to exchange a password for the API key, `GET /` redirect to `/phone`, and made `GET /api/device-status` publicly accessible without auth
