### Fixed

- **Fix UiMono bold text compositing** — `text_mono_bold_scaled_mixed` was incorrectly using `FlatBackground` mode instead of `MixedBackground`, causing inconsistent text rendering weight for bold monospace UI text.
