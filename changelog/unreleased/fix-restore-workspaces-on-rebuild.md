### Fixed
- **Restore workspaces after rebuild/reinstall** — When the daemon has no live sessions but workspace definitions are persisted, the shell now loads and restores those workspaces with their original names, folder paths, and worktree mode instead of defaulting to a single "Workspace 1"

