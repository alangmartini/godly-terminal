### Changed
- **Sidebar visual redesign** — Active workspace now has 3px accent-colored left bar, accent-tinted background, and bold font weight for clear visual hierarchy. Header redesigned with uppercase "WORKSPACES" in terminal font and separator line. Context menu elevated with shadow and rounded hover states. Footer gets terminal-flavored "//" prefix with brightening on hover. Notification dot has subtle shadow glow. AI mode badge wrapped in styled chip container. Resize handle now visible with border color. All magic numbers replaced with design tokens (SPACE_*, RADIUS_*, UI_FONT_SIZE*). Worktree indicator uses stronger accent color (#YYY)
