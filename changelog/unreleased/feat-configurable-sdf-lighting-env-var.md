### Added

- **Configurable SDF surface lighting** — Set `GODLY_LIGHTING=0.3` (0.0–1.0) to enable the dormant SDF lighting model on UI chrome elements (specular highlights, rim light, ambient occlusion, glass-edge effect). Default 0.0 preserves the current flat aesthetic.
