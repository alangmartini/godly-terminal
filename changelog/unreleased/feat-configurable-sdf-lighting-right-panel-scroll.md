### Added
- **Right panel scroll support** — poem content in the right panel is now scrollable via mouse wheel, with a custom scrollbar thumb matching the web reference CSS styling (#2d333b, 6px wide, 3px radius)
