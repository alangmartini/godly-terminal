### Added
- **Cursor blink animation** — streaming cursor in reference pane now blinks with 1s period matching web reference (`animation: blink 1s infinite`)

### Fixed
- **Tilde pulse timing** — status bar `~` pulse corrected from ~5.8s period to web-accurate 1.5s period with 0.6–1.0 opacity range
