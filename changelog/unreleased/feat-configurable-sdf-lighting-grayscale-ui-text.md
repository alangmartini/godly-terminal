### Changed

- **UI text rendering switched to grayscale AA** - Proportional UI font (Segoe UI) now uses grayscale anti-aliasing and sRGB gamma compositing instead of ClearType subpixel rendering, matching browser text rendering weight for closer visual parity with the web reference.
