### Changed
- **UI chrome text switched to monospace** — tab titles, sidebar session names, status bar labels, agent panel text, and shortcut labels now render in Cascadia Code (monospace) matching the web reference's root `fontFamily` instead of Segoe UI (proportional)
