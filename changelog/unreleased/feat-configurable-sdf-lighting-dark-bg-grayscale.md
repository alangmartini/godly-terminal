### Changed
- **Grayscale AA on dark backgrounds** — Text on dark backgrounds (luminance < 0.5) now uses grayscale anti-aliasing instead of ClearType subpixel blending, eliminating color fringing and matching browser rendering behavior
