### Fixed
- **Quick Claude resume syntax** — Fixed `claude --resume` command syntax to not include `--session-id` flag in the command string itself. Session ID is passed as a positional argument (feat/quick-claude-resume-worktrees)
