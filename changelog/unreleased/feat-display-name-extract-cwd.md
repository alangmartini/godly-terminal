### Added
- **Friendly terminal labels** — New `display_name()` utility strips executable paths and maps shell types to user-friendly names (e.g., "PowerShell", "Bash" instead of full paths)
- **CWD extraction** — New `extract_cwd()` utility extracts current working directory from OSC title for status bar display
