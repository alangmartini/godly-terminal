### Fixed
- **Status bar element spacing** — replaced `" | "` string separators with explicit 6px gap spacing between right-side status bar elements (path, branch, diff stats) to match web reference's `gap: 6` flex container layout
