### Added
- **Multi-stop gradient support in SDF shader** — Fragment shader computes piecewise linear interpolation between up to 3 color stops with configurable direction and mid-stop position. Progress bar now uses single 3-stop gradient matching web's `linear-gradient(90deg, #6366f1, #8b5cf6, #6366f1)`.
