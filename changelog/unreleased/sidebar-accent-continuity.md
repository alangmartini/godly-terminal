### Changed
- **Sidebar accent color continuity** — Active workspace indicator, border, and glow now use the session's own accent color from the rotating palette instead of hardcoded blue, creating consistent color flow from tab badge → sidebar dot → active indicator
- **Terminal branding icon** — Added terminal monitor icon with prompt caret next to "Godly Terminal" text in the title bar
- **Sidebar metadata readability** — Boosted branch and description text base luminance for better readability at rest
